# Video_Parser_Extension
Chrome扩展 影视解析辅助工具

```
https://im1907.top/?jx=
https://jx.xmflv.com/?url=

写一个chrome扩展：
添加后台设置项，增加影视解析接口，不限个数。
前台提供功能按钮
点击功能按钮后，1.捕获当前标签地址栏内url，将解析接口url+地址栏捕获的url组合成新的url 2.当前标签自动跳转到新的url
每个影视接口对应一个前台功能按钮


以下错误需要修正：
1.增加解析接口设置项独立放在配置页，而不是前端
2.新增解析接口url增加保存按钮，使配置能够存储
3.前端增加"播放"按钮，点击后1.捕获当前标签地址栏内url，将解析接口url+地址栏捕获的url组合成新的url 2.当前标签自动跳转到新的url


以下错误需要修正：
html中文乱码
前端需要多个播放按钮，每个解析接口对应一个播放按钮

非常好，继续优化：
1.在options页为每个影视解析接口添加移除按钮，用于删除储存。为每个接口编号
1.在popup页播放按钮添加编号，与接口编号对应

以下错误需要修正：
1.移除按钮不工作，点击后并没有删除对应接口 请修复
2.点击播放按钮后，修改为在新标签打开 而不是当前标签
```
